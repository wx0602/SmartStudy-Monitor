{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run SmartStudy (debugpy)",
            "type": "debugpy",
            "request": "launch",
            "module": "app.main",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
